"use client";

// STUB — implémenté en PR 4.
// Lenis smooth scroll + GSAP ScrollTrigger pipe.
// Désactivé si prefers-reduced-motion: reduce.

import { ReactNode } from "react";

export function SmoothScrollProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
